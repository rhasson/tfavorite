div.list
  each item in favs
    div.el_item.span10
      div.profile_pic: img.img-polaroid.img-rounded(src='#{item.user.pic}')
      div.profile_header
        span.screen_name
          h5 @#{item.user.screen_name}
          h6 (#{item.user.name})
        span.text #{item.text}